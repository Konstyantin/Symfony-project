{% extends 'base.html.twig' %}
{% block body %}
    <div class="question-title">
        <p>Question</p>
    </div>

    <div class="question-container">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
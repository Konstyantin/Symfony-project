actor: AcceptanceTester
modules:
    enabled:
        - WebDriver
        - \Helper\Acceptance
    config:
        WebDriver:
            url: 'http://symfony-project.com/'
            browser: 'chrome'

env:
    chrome:
         modules:
            config:
                WebDriver:
                    browser: 'chrome'